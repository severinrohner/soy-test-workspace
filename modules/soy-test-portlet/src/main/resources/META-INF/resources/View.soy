{namespace View}

/**
 * Prints the Hello Soy portlet main view.
 *
 * @param id
 * @param? releaseInfo
 */
{template .render}
        <div id="{$id}" >

            <h1>Current Portal Version:</h1>
            <p>{$releaseInfo}</p>
            <button data-onclick="myClickFunction" type="button">Fast Portal Update ;-)</button>

        </div>
{/template}